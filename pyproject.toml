[project]
name = "lerobot-latent-actions"
version = "0.1.0"
description = "Utilities for offline latent-action labeling of LeRobot datasets."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
